# 待你處理的事項（程式碼以外）

> 整理日期：2026-07-30
> 這份只列**我不能替你做**的事：憑證操作、平台申請、伺服器操作，以及需要業務或法務拍板的決定。
> 程式碼側的進度見 [實作計畫.md](./實作計畫.md)。

---

## 一、最急 —— 不處理的話「現在就會壞」

| # | 事項 | 為什麼急 |
|---|---|---|
| 1 | **把 [`deploy/schema.sql`](./deploy/schema.sql) 套用到線上資料庫** | 2026-07-30 實測連上 `sxk_db`：`expert_bookings`、`payments`、`unlocks`、`sms_codes` **四張表都不存在**。專家預約功能一上線就是 500。**先備份 RDS**，檔尾的 `ALTER` 遷移語句確認後再執行 |
| 2 | **建立 `sxk_t1_db` 並套用同一份 schema** | 專案 B 的獨立資料庫。同一個 RDS 實例，零額外成本 |
| 3 | **Render 兩個 service 填入 AI 金鑰**（`DASHSCOPE_API_KEY`、`GEMINI_API_KEY`） | 沒填的話四個 AI 供應商全部失敗，系統靜靜改用本地模板。家長拿到的不是 AI 報告，而且**畫面上不會有任何提示** |

> 第 3 項是憑證操作，必須你本人在 Render 後台的 Environment 頁面做。

---

## 二、憑證與帳號

| # | 事項 | 說明 |
|---|---|---|
| 4 | **`WECOM_WEBHOOK_URL`**（企業微信群機器人） | **最便宜的一項**：一個網址，免申請免費用。沒有它，專家預約會寫進資料庫但**沒有任何人會知道**。A/B 兩個 service 都要 |
| ~~5~~ | ~~**客服微信號 + 二維碼圖片**~~ | ✅ **2026-07-31 已完成**：企業微信二維碼放在 `public/kefu-qr.jpg`，A／B 兩個模式都已指過去。不列微信號（企業微信客服只能掃碼，手打帳號走不通） |
| 6 | **`SESSION_SECRET`** | Render 已自動產生；阿里雲那邊要自己設一個長隨機字串，否則每次重啟所有人都被登出 |
| 7 | **輪換 DashScope key 與資料庫密碼** | `.env` 從未進過版控（已查證），但曾存在於多個工作目錄。建議輪換 |

---

## 三、微信支付 —— 有先後順序，總工期以「週」計

程式已寫好（H5）。以下每一項都是**硬性前置**，缺一項就無法收款。

| # | 事項 | 工期 / 備註 |
|---|---|---|
| 8 | **確認手上有沒有「已認證」的公眾帳號** | ⬅️ **最先做這件事**。官方明文「未认证的账号无法绑定商户号」。而且這決定 H5 還是 JSAPI —— **JSAPI 必須是已認證服務號** |
| 9 | **商戶號（mchid）申請** | 企業／事業單位主體，個人不可。用**虛擬服務類目**申請，不要綁商城實體商品（見第 29 項） |
| 10 | **ICP 備案完成** | 進行中。審核期間阿里雲會來電核驗，**務必接聽**，否則退件重來 |
| 11 | **商戶平台配置「H5 支付域名」** | **審核 3～5 個工作天**。備案主體須與商戶號主體一致 |
| 12 | **APIv3 密鑰** | 商戶平台設定，必須恰為 32 位字母數字 |
| 13 | **商戶 API 證書** | `apiclient_key.pem`（私鑰）+ 證書序列號 |
| 14 | **微信支付公鑰 + 公鑰 ID** | 選**公鑰方案**（官方推薦），不要用舊的平台證書 —— 可以省掉整套證書下載與輪換邏輯 |
| 15 | **公網可達的 HTTPS `notify_url`** | 不可帶查詢參數、不可為 localhost 或內網 IP |

拿到後填入環境變數，清單與說明在 [`.env.example`](./.env.example)；Render 的欄位已在 [`render.yaml`](./render.yaml) 定義好（專案 B 刻意沒有任何 `WECHATPAY_*`）。

⚠️ **憑證由你本人設定，不進版控。**

---

## 四、阿里雲與伺服器

| # | 事項 |
|---|---|
| 16 | 主機把服務跑起來：`pm2 start deploy/ecosystem.config.js`，設好 `MYSQL_*` 與 `APP_MODE` |
| 17 | 修 [`deploy/nginx.conf`](./deploy/nginx.conf)：`server_name` 仍是佔位符 `your-domain.com`，改成真實網域並加上子網域 server block |
| 18 | 備案通過後申請 HTTPS 憑證，啟用 nginx 中被註解的 HTTPS 區塊 |
| 19 | **建置改在本機或 CI 做**：主機只有 2 GiB RAM，兩個 PM2 程序加上 vite/esbuild 同時跑容易 OOM |

---

## 五、阿里雲簡訊（審核 1～2 個工作天）

| # | 事項 | 用途 |
|---|---|---|
| 20 | **簡訊簽名 + 模板送審** | 兩個用途：專家預約通知的第二軌（目前刻意未實作傳輸層，因為沒有憑證就無法驗證簽名程式是否正確）；以及之後的**簡訊驗證碼登入** |

---

## 六、需要你拍板的決定

這些我不做決定 —— 是業務或法務層級的判斷。詳細論證見 [與客戶討論的問題.md](./與客戶討論的問題.md)。

| # | 決定 | 出處 |
|---|---|---|
| 21 | **「一鍵模擬 10 題」去留** —— 移除／僅開發環境／保留但報告標示 | 第九項（新增） |
| 22 | **語言專項那三位「治療師」是不是真人、有沒有授權** —— 現在預約是真的了，他們會開始收到真實預約單 | 與第六項相關 |
| 23 | **報告頁三位醫師是否知情同意接受真實預約**，以及「10 分鐘內電話回訪」這句承諾能否履行 | 第六項 |
| 24 | **H5 還是 JSAPI** —— 取決於你的家長從哪裡點進來（微信內／微信外） | 見下方「要給我的資料」 |
| 25 | T2/T3 是否繼續使用真實臨床量表名稱（Bayley-4、SIPT 等） | 第一項 |
| 26 | T2/T3 的真實題目由誰編製、時程、誰簽核 | 第二項 |
| 27 | 隱私條款四項承諾要不要實作（HTTPS、監護人同意、第三方分享同意、撤回權） | 第三項 |
| 28 | 專案 B 要不要登入（目前暫定不登入） | 第五項 |
| 29 | 商城實體商品的經營資質（醫療器械／保健食品／醫學檢驗） | 第八項 |
| 30 | AI 端點無身分驗證的成本由誰承擔（B 免費、不登入、交付合作公司） | 第四項 |
| 31 | **¥19.9 的退款政策** —— 資料層已備好（退款會撤銷權益），但沒有觸發它的流程。誰能退、退多久內、怎麼申請 | 新增，做付費後才浮現 |

---

## 七、要提供給我的資料

| 資料 | 我拿它做什麼 |
|---|---|
| **微信內流量佔比的估計** | 決定 H5 / JSAPI。若家長多半從微信群或公眾號點進來，**只做 H5 等於主要通路全部付不了款** |
| **客服微信二維碼圖檔** | 放進 `public/`，接到專家預約成功畫面 |
| **三位專家真實可服務的時段** | 目前時段是寫死的固定四段；預約成真之後，這是對家長的承諾 |
| **¥19.9 的商品說明文案**（若法務要改） | 目前是我寫的：T2 量表 + T3 實測 + AI 專項報告 + 永久有效可免費重測 |
| **退款政策文字** | 付費頁必須寫清楚 |
| **T2/T3 真實題目**（若已編製） | 格式：維度 × 層級 × 年齡段。到位後可一併解掉「內容債」（目前三個維度的 T2/T3 內容仍停留在舊分類體系） |

---

## 附：目前哪些東西「沒設定也不會壞」

避免誤會 —— 以下缺著不影響現有功能，只是功能未啟用：

- **微信支付憑證全空** → 訂單仍可開立，前端顯示「支付通道尚未開放」並附訂單號，**不會給假的成功**
- **`MYSQL_*` 未設定** → 付費牆不執行、深度評估全部放行（展示模式）。啟動日誌會明講：`Paywall NOT enforced ... Demo mode only`
- **`WECOM_WEBHOOK_URL` 未設定** → 預約仍會寫入，通知失敗時日誌會大聲喊 `Expert booking N reached NOBODY`
