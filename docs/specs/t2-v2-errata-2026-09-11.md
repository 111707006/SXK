---
status: pending-rewrite
date: 2026-09-11
for: t2-v2-sxk-toolkit-rules-engine-report-and-activities.md
owner: 使用者（整份規格改版時一併回寫；回寫完刪掉本檔）
---

# T2 規格 v2 勘誤：實作時發現的前後矛盾與程式碼的暫採

> **這是什麼**：實作 #42、#43 時，規格有幾處自己兩段話對不上。程式碼每一處都取了一邊、
> 釘在測試裡；這裡把「哪兩段、選了哪邊、為什麼、改版時要動哪裡」列成一張表，
> 讓規格改版可以一次做完，不必再回頭翻 commit。
>
> **怎麼讀**：每條的「程式碼現在」就是正式行為（commit 記在條目裡）。「改版時」是把規格改成
> 與程式碼一致的最小改法；若使用者改版時決定反過來，要動的程式碼也寫在那一欄。
>
> 使用者 2026-09-11：「記錄起來，我整份寫好一起改。」—— 所以本檔**不動規格本文**，只在規格
> 檔頭放一行指過來。

---

## A. #43 路由（`src/t2/routing.ts`，commit 7aea1dc）

### A1. 星號要不要給 `sxk-dev`（影響家長看到的必做）

| | |
|---|---|
| 規格說 A | 附錄 B.1：LANG 37–72 星號 **sxk-dev**；COG 0–36「12–35 個月時星號是 sxk-dev」；COG 37–72 星號 **sxk-dev**。§4.2 演算法第 3 步「星號＝候選[0]」配客戶表語言 3–6 的第一支 DEV，機械上會得到這個結果 |
| 規格說 B | §4.4 例子「48 個月、LANG 紅、ATT 紅、SEN 黃 → 必做 sxk-lang 49 ＋ sxk-ab 41 ＝ 90 題」；§11 階段 3 的固定輸入；票 #43 驗收第 3 條 |
| 程式碼現在 | 取 B。`candidatesFor` 多一條「`sxk-dev` 恆排候選末尾」（§4.2 沒有的第 4 條）。理由：dev 每個領域 5 題、永遠帶 `few_items`，是 T2 裡的廣篩；擋在 49 題的專用工具前面當「必做」說不過去 |
| 後果 | LANG 37–72 星號 dev → **lang**；**COG 18–72 星號 dev → adp**（36 個月仍是 asq；0–17 仍是 dev）；MOT 0–84 加測順序 dev 退到 asq／adl 之後 |
| 改版時 | §4.2 加第 4 條「`sxk-dev` 恆排候選末尾」＋一句理由。B.1 改四格：LANG 37–72 →「**sxk-lang**、〔asb〕、〔asr〕＋ sxk-voc（≤42）、sxk-asq（≤42）、sxk-dev」；COG 0–36 →「**sxk-asq**（36）、〔voc〕＋ sxk-adp（18＋）、sxk-dev——0–17 星號 dev、18–35 adp」；COG 37–72 →「**sxk-adp**、sxk-dev ＋ sxk-asq（≤42）——dev 退到末尾後的順序是 adp、asq（≤42）、dev」；MOT 0–84 →「**sxk-gm**（6–72）、sxk-asq（36–42）、sxk-dev——0–5 星號 dev」（adl 見 A2）。§10.2 加一列 |
| 若反過來 | 刪 `candidatesFor` 最後一行的 dev 移位；改 `test/t2Routing.test.ts` 的 `SPEC_B1` 四格、「星號隨月齡變」的 LANG／COG 兩條、固定輸入那組（必做會變成 dev 30 ＋ ab 41）。§4.4、§11、票 #43 的 90 題也要跟著改 |

### A2. B.1 列了、附錄 F 說「不餵那個維度」的三格（影響 7 歲以上的動作）

| | |
|---|---|
| 規格說 A | B.1：LANG 0–36 列 mchat-rf（16–30）不加〔〕；MOT 0–84 列「sxk-adl（30＋，只有 MO 4 項，`few_items`）」、MOT 85–216 星號 **sxk-adl**（≤180）；ADL 37–72 列「sxk-asq（≤42）」。§4.5 表「動作 85–216 → 只剩 sxk-adl 的『移動與轉位』4 項」 |
| 規格說 B | 附錄 F：`mchat-rf` 只餵 SOC、`sxk-adl` 只餵 ADL、`sxk-asq` 餵 LANG／MOT／COG／SOC（沒有 ADL）。附錄 C 那列：「sxk-adl（頂 PedsQL 的格子）：動作 85–216 … 本規格：日常生活」。§4.2 第 2 條「過維度對應（附錄 F）」 |
| 程式碼現在 | 取 B（附錄 F；`toolSpecs.ts` 的 `TOOL_FEEDS` 在 #42 就照 F 寫了）。mchat 不是 LANG 的候選、adl 不是 MOT 的候選、asq 不是 ADL 的候選 |
| 後果 | **MOT 85–216 是 no_tool**——§4.5 的洞表沒列它。7 歲以上動作紅的孩子看到「此年齡尚無適用工具」；若他只有動作被標記，依 §4.5 不顯示 T2 入口。LANG 0–36 少一支加測（mchat）；ADL 37–72 少一支加測（asq） |
| 改版時 | B.1：LANG 0–36 的 mchat-rf 加〔〕；MOT 兩格的 sxk-adl 加〔〕，MOT 85–216 改成「〔spb〕、〔adl〕→ **no_tool**」；ADL 37–72 刪 sxk-asq。§4.5 表補一列「動作 85–216 ｜ SXK-SPb、SXK-ADL ｜ 空（adl 只餵日常生活，附錄 C）」並刪「只剩 sxk-adl 的移動與轉位 4 項」；§4.5 決定段的 no_tool 清單加 MOT 85–216 |
| 若反過來 | 這是 📞 §10.1 第 6 題（維度對應收窄）的範圍。要讓 adl 的 MO 餵 MOT：改附錄 F、`TOOL_FEEDS['sxk-adl']` 加 `{ dimension: 'MOT', sections: ['MO'] }`、附錄 C 那列、`test/t2ToolSpecs.structure.test.ts` 的 feeds 重抄表、`test/t2Routing.test.ts` 的 MOT 兩格與「MOT 73＋ no_tool」那條 |

### A3. 段界那一個月：36 與 72

| | |
|---|---|
| 規格說 A | §4.5 洞表：ATT 12–36、LEARN 37–72、EMO 0–72 是 no_tool。B.1 這三格沒寫「＋」後備 |
| 規格說 B | §4.2 第 3 條「附錄 F 餵該維度、在窗口內、DIM 沒列的補在末尾」；§3 窗口 sxk-ab 36–192、sxk-ldp 72–144、snap-iv 72–216。§4.5 自己也寫「只有 snap-iv 的 OD 在 72 個月起」 |
| 程式碼現在 | 取 B，逐月照窗口。36 個月 ATT 紅 → 必做 sxk-ab；72 個月 LEARN 紅 → sxk-ldp；72 個月 EMO 紅 → snap-iv。no_tool 是 ATT 0–35（0–11 客戶表無路由）、LEARN 0–71、EMO 0–71 |
| 改版時 | B.1：ATT 12–36 →「〔tempa〕、〔spa〕＋ sxk-ab（36）——0–35 no_tool」；LEARN 37–72 →「…＋ sxk-ldp（72）——37–71 no_tool」；EMO 37–72 →「…＋ snap-iv（72）——0–71 no_tool」；ATT 37–72 補「＋ snap-iv（72）」；SEN 37–60 補「＋ sxk-spb（60）」。§4.5 洞表的月齡改成 ATT 0–35、LEARN 0–71、EMO 0–71 |

### A4. SOC 的星號是四段不是三段

| | |
|---|---|
| 規格說 A | B.1 SOC 0–36：「0–15 個月時星號是 sxk-dev；31–35 個月時是 sxk-soc」；票 #43 驗收「SOC 0–15／16–30／31–35」 |
| 規格說 B | §3：sxk-soc 窗口 12–72。B.1 同一格的後備順序寫的就是 soc、asb、asr、dev（soc 在 dev 前） |
| 程式碼現在 | 取 B：0–11 dev、12–15 **soc**、16–30 mchat-rf、31–35 soc、36 asq。「0–15」是把 mchat 的起點（16）當成了整格的起點 |
| 改版時 | B.1 SOC 0–36 那句改成「0–11 星號 sxk-dev；12–15 sxk-soc；16–30 mchat-rf；31–35 sxk-soc；36 sxk-asq」 |

### A5. 診斷方向點到不出 band 的工具（chexi、tempa、tempb）

| | |
|---|---|
| 規格說 A | §4.3「把 DIS 那格的工具全部提為必做」；B.2 裡 dd／id 0–36 有 tempa，ld／adhd／tic 三段有 chexi，cp／dd／id 145–216、lang／emo／psych／asd 多格有 chexi |
| 規格說 B | §4.2 第 1 步「只出標籤的工具另列 extras：永遠是『選做』」；附錄 A `T2Plan.estimatedItems` 沒有 extra 那一格 |
| 程式碼現在 | 取 B：DIS 裡不出 band 的落到 `extras`（role `'extra'`），`forDimensions` 依附錄 F；不擋報告、不進題數 |
| 改版時 | §4.3 加一句「DIS 裡不出 band 的（chexi、tempa、tempb）列為 extras，不提為必做」；§4.2 第 4 步同步 |

### A6. extras 的來源（要不要過附錄 F）

| | |
|---|---|
| 規格說 A | 附錄 F 說明句：「`producesBand=false` 的工具 `feeds` 只用來決定標籤歸哪個維度與 extras 的顯示」→ 只看 feeds 的話 tempa／tempb 只在 EMO、chexi 只在 ATT |
| 規格說 B | B.1 斜體：ATT 12–36 *sxk-tempa*、ATT 37–72 *sxk-tempb*、LEARN 73–144 *chexi*——feeds 全對不上，但客戶表在那些格列了它們 |
| 程式碼現在 | 兩邊都收：客戶表在該段列了就列（**不過**第 2 條，它們不出 band，feeds 對它們不決定任何事），加上附錄 F 餵該維度的；都要在窗口內。B.1 LEARN 37–72 的 *chexi*（48＋）**沒有任何一條規則產得出**（客戶表學習 3–6 沒列、F 只餵 ATT），不收 |
| 改版時 | §4.2 第 1 步 extras 那句改成「客戶表在該段列了、或附錄 F 餵該維度，且在窗口內；永遠是選做」；附錄 F 說明句的「與 extras 的顯示」刪掉（那句的另一半「決定標籤歸哪個維度」也是錯的，見 B4）。B.1：LEARN 37–72 刪 *chexi*；EMO 0–36 補 *sxk-tempb*（36）；EMO 73–144 補 *sxk-tempb*（≤84）；LEARN 145–216 補 *chexi*（≤155，客戶表有列） |

### A7. 後備的先後（影響最小）

| | |
|---|---|
| 規格說 A | §4.2 第 3 條「依附錄 F 的順序」（voc 在 asq 前） |
| 規格說 B | B.1 LANG 37–72「＋ sxk-lang、sxk-asq（≤42）、sxk-voc（≤42）」 |
| 程式碼現在 | 取 A。dev 排末尾後兩支都在三支之內，先後只影響顯示順序 |
| 改版時 | 隨 A1 重寫那格時寫成 voc、asq |

### A8. 不算矛盾，B.1 沒寫全（改版時順手補）

- LANG 0–36 在 36 個月多一支後備 sxk-asq（CO 餵 LANG、窗口 36–42）→ 補「＋ … sxk-asq（36）」。
- SOC 37–72 在 37–42 多一支 sxk-asq（PE 餵 SOC），排第四、被三支上限截掉 → 補「sxk-asq（≤42）」。
- 票 #43 驗收「LANG 0–11 vs 12＋」：實際是三段——0–11 dev、12–36 voc、37–72 lang。

---

## B. #42 字彙層（`src/t2/toolSpecs.ts`、`findingTags.ts`，commit f67fe17）

已在該 commit 訊息與測試裡記錄，這裡只列標題讓改版一次抓完。

| # | 兩段 | 程式碼現在 | 改版時 |
|---|---|---|---|
| B1 | §5.5 sen 那列自己列了 11 個標籤，超過 §5.5／CONTEXT.md「每維度 ≤10」 | 照表 11 個（三個 `impact_*` 在 §5.1、§5.9 都被引用，砍了 spa／spb 前置題產不出東西） | 把「≤10」改成「≤10，sen 例外 11」或砍一個並同步 §5.1、§5.9 |
| B2 | §5.6「18 支自建工具一律帶 `unsourced_threshold`」（18 含氣質）vs §5.9 氣質那列寫「無」 | 取 §5.6：tempa／tempb 帶 | §5.9 氣質列補 `unsourced_threshold` |
| B3 | §5.6 列舉句把 voc 算進 `parent_administered_task`，§3「家長」欄與 §5.9 都沒有 | 取 §3／§5.9：voc 不帶 | §5.6 那句刪 voc |
| B4 | 附錄 F 說明句「feeds 決定標籤歸哪個維度」對好幾支是錯的（氣質 feeds EMO 卻出 `att.inattention`、`sen.threshold_low`、`learn.task_persistence`；chexi feeds ATT 卻出 `emo.regulation`……） | 標籤的維度一律看前綴（`tagDimension()`），不看 feeds（`types.ts` 的 `ToolFeed` 註解） | 附錄 F 那句改成「`feeds` 只決定 band；標籤歸哪個維度看標籤自己的前綴」 |

---

## C. #44 活動種子（`src/t2/activitySeed.ts`、`deploy/migrations/2026-09-11-activities.sql`）

### C1. 附錄 B.3 說自己是「反查」，但不是 §7.2 `DIM_MOD` 的反查

| | |
|---|---|
| 規格說 A | 附錄 B.3 標題「模組 → 維度初值（活動種子用）」，§7.1 `dimensions` 註解「從模組推初值（附錄 B.3 的反查）」；票 #44「維度初值依附錄 B.3 從模組反查」，驗收點名「模組 4 → MOT＋ADL；模組 7 → LANG＋ATT＋SOC＋COG」 |
| 規格說 B | §7.2 `DIM_MOD` 真的反查出來不是那張表：模組 4 只有 MOT（ADL 的模組是 3、5、6、9、12、15，沒有 4）；模組 9 是 LEARN／ATT／LANG／SOC／EMO／ADL（沒有 COG）；模組 2 多 LEARN、3 多 ADL、11 多 SEN／LANG、12 多 COG／LEARN／ADL、14 多 SEN、15 多 SOC／EMO／ADL |
| 程式碼現在 | 取 A：`MODULE_DIMENSIONS` 照 B.3 字面抄 15 格，`test/activitySeed.test.ts` 重抄一次釘住。理由：票的驗收點名 B.3 的值；而配對（§7.3）看的是 `moduleNo ∈ DIM_MOD[d]`，**不看 `Activity.dimensions`**，所以這個欄位只影響後台顯示與篩選，不影響哪個孩子拿到哪支活動 |
| 改版時 | B.3 的「反查」兩個字拿掉，改成「模組 → 維度初值（人工給的初值，不是 `DIM_MOD` 的反查；配對不看這個欄位）」；§7.1 那句註解同步。或反過來讓 B.3 ＝ `DIM_MOD` 反查，那要改 `MODULE_DIMENSIONS` 15 格中的 8 格與測試的 `B3` 表，並改票 #44 驗收第 4 條 |

### C2. 「全龄」不在 §7.1 的解析規則裡

| | |
|---|---|
| 規格說 | §7.1「`ageMonths` 從『3–8岁』解析」；票 #44 只列了三種字串與「解析不了的字串讓測試失敗」 |
| 原型有 | 300 支裡兩支不是「X–Y岁」：第 20 支「每天动一动」是 `全龄`，第 300 支「一起长大的每一天」是 `全龄／收官` |
| 程式碼現在 | `全龄`（含 `／收官` 備註）是明確的記號，解析成 `{ min: 0, max: T1_AGE_RANGE.maxMonths }`（現在是 0–180）。上限不另發明數字，直接用平台服務的最大月齡；其他任何字串照票的要求丟例外 |
| 改版時 | §7.1 補一句「`全龄` → 0 到平台最大月齡；`／收官` 是備註，去掉」 |

### C3. 種子沒有的欄位怎麼填（不算矛盾，規格沒說）

- `durationMin` 型別是 `number` 不可 null（§7.1），種子填 **0**，讀作「還沒填」。`targetMonth` 才是規格明說可 null 的那一個。
- `equipment`、`steps`、`targets`、`avoidIf` 全空陣列；`videoUrl` null；`active` true。
- `id` 照 §7.1 的 `'A017'`：`A` 加三位數。

---

## D. 改版時順帶要對的地方

- §4.3「三格是空的：選了就等於沒選」只涵蓋客戶留白的格。客戶有列工具、但在該月齡全在窗口外的格
  （自閉症 0–5 個月、情緒障礙／心理疾病 0–11 個月）：程式碼**不提任何工具、但 `functionOrder` 照客戶給的回傳**
  （報告仍依診斷方向排維度）。§4.3 補一句把這兩種情況分開。

- §4.5「所有被標記的維度都是 `no_tool` 時，不顯示 T2 入口」：A2 之後 7 歲以上「只有動作被標記」也會落到這一條，📞 §10.1 第 5 題帶去給客戶時把這格算進去。
- §10.2 補兩列：A1（dev 恆排末尾）、A5（DIS 裡不出 band 的不提為必做）。
- 票 #43（GitHub issue）的驗收文字：第 2 條的 `no_tool` 月齡、第 4 條的 SOC 段數、補 MOT 85–216。
- 票 #44（GitHub issue）的驗收文字：適齡解析那條補「全龄」；「維度初值依附錄 B.3 從模組反查」的「反查」隨 C1 改。
