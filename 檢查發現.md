# 全庫檢查發現（白話版）

> 檢查日期：2026-07-29
> 對象：整份程式碼 14,631 行，加上 Render 線上四個服務
> 這份刻意不寫函式名稱與技術術語，只講「使用者會遇到什麼」。
> 技術細節與檔案位置放在每一項的最後一行，給工程用。

---

## 檢查當下的健康狀況

| 項目 | 結果 |
|---|---|
| 型別檢查 | 通過，沒有錯誤 |
| 自動化測試 | 40 項全部通過 |
| 專案 B 的建置 | 成功 |
| 專案 A 線上版本 | 確認就是 A（實際下載線上檔案驗過） |

**所以整體是健康的。** 以下三項是檢查中翻出來的問題，不是系統壞掉，而是「有東西沒接起來」。

---

## 發現一：最大的一個功能，使用者永遠點不到 🔴 ✅ 2026-07-30 已處理

> **決議：接上入口，不刪。** 報告頁在「語言溝通」被標記為臨界或迟缓時出現進入按鈕，
> 並走與其他維度相同的付費解鎖檢查（未解鎖 → 付費牆）。
>
> 順帶修掉裡面兩件事：
> 1. **預約是假的** —— 原本 1.2 秒後生一張憑證，含**憑空產生的騰訊會議連結與密碼**。
>    家長會拿著一個不存在的房間號在約定時間乾等，而沒有任何人知道有這筆預約。
>    現在走與報告頁同一支 `/api/expert-booking`，真的寫入並通知；會議連結不再產生，
>    改為「由客服在電話確認時間後提供」。
> 2. **少收家長姓名**（後端必填），送出必定被擋；`alert()` 改為畫面內錯誤訊息。
>
> ⚠️ **仍待你確認**：裡面三位「治療師」（林舒涵教授等）的簡歷寫法看起來是虛構或
> 去識別化的，與報告頁三位可查證的真實醫師不同性質。現在預約是真的了，這三位會
> 開始收到真實預約單 —— 名單要不要換成報告頁的 `SPECIALISTS` 是產品決定。

### 以下為當初的記錄

### 白話說明

系統裡有一個「語言專項深度評估」的完整頁面。它是**整個專案裡程式碼最多的單一功能**，比首頁還大，佔了將近 2,400 行。

問題是：**沒有任何按鈕能打開它。**

不是被權限擋住，也不是 B 版本才這樣 —— 是 A 和 B **兩個版本都一樣**，從頭到尾找不到入口。程式碼裡確實準備好了「當使用者點下去時要做什麼」，但那個「點下去」的按鈕從來沒有被放到畫面上。

打個比方：房子蓋好了、裝潢完了、水電都通了，但外牆沒有開門。

### 影響

- 這是**專案 A 的功能缺失**。付費版本少了一個已經做完的功能。
- B 版本本來就不該有這個功能，所以對 B 沒有損失。
- 這 2,400 行仍然會被打包進網站檔案裡，佔約 82 KB 的下載量，卻沒有人用得到。

### 要決定的事

**接上入口，還是整段刪掉？** 兩個都合理：

- **接上**：A 多一個賣點，工作量小（就是把按鈕放回畫面上）。
- **刪掉**：少維護 2,400 行，網站檔案變小。

> 技術位置：`src/components/AnalysisReport.tsx:78` 與 `:82` 宣告了跳轉用的參數但函式本體從未使用；`src/App.tsx:804`、`:815` 傳入的跳轉動作因此永遠不會被觸發，`src/App.tsx:1041` 的畫面永遠到不了。目標檔案 `src/components/LanguageSpecialAssessment.tsx`（2,388 行）。

---

## 發現二：B 版本的伺服器，還開著 A 版本的四個 AI 通道 🔴 ✅ 2026-07-30 已處理

> 伺服器啟動時就依 `APP_MODE` 決定要不要註冊這些通道，B 直接不開（實測回 404）。
> 付費相關的通道（下單、查單、微信回調）後來也加在同一個開關上 —— 這正是當初
> 建議「先建好分 A/B 機制再做付費」的理由。

### 以下為當初的記錄

### 白話說明

A 和 B 的差別，目前**只做在畫面上**：B 的使用者看不到深度評估的按鈕，也點不進去。

但**伺服器那一端完全沒有分 A 和 B**。B 的伺服器上，四個原本只給深度評估用的 AI 服務通道仍然是開著的：

- 深度評估報告產生
- 語言能力評測
- 動作影片評估
- 動作評估報告

而且這四個通道**不需要登入就能呼叫**，只有「同一個網路位置十分鐘內最多兩百次」這個限制。

### 影響

用比喻來說：你把商店的「深度評估」商品從貨架上拿走了，但**後門的倉庫還是開著的**，知道路的人可以直接進去拿，而且拿的每一次都在燒你的 AI 帳單。

這件事在交付給合作公司之後特別要緊 —— B 是免費產品、不需登入，AI 費用完全由森心康承擔，而合作月費是固定的。

### 建議

伺服器啟動時就依照「這台是 A 還是 B」決定要不要開這四個通道。B 直接不開，比「開了再擋」更安全。

**這件事必須在做付費功能之前完成。** 因為付費相關的通道會加在同一份共用程式碼裡，如果「分 A/B」這個機制沒有先建好，付費通道一加進去就是預設對外開放的狀態。

> 技術位置：`server.ts` 完全沒有讀取 `APP_MODE` 環境變數。相關端點 `/api/specialized-report`、`/api/ali-language-eval`、`/api/motion-eval`、`/api/motion-report`。與 `實作計畫.md:138` 標註的待辦一致。

---

## 發現三：B 版本的九張維度卡片，點了沒反應 🟡 ✅ 2026-07-30 已處理

> 卡片改為可點，導向報告頁的專家諮詢區塊。已實機確認 B 的卡片顯示「联系专家」、
> 點下去進報告頁；**自動捲到預約區塊那一小段沒能實機確認** —— 平滑捲動在無畫面的
> 測試環境不會執行（實測改成瞬間捲動就會動）。

### 以下為當初的記錄

### 白話說明

在 B 版本，家長做完篩查後會看到九張維度卡片。原本的設計是：**點下去應該要引導他去聯繫專家**。

目前的實際行為是：卡片被設成不可點擊，點了完全沒反應。

系統裡其實已經記錄了「B 版本點下去應該去聯繫專家」這個設定，但**沒有任何一段程式去讀它**。設定寫了，沒人用。

### 影響

B 的整個商業目的是「產生專家諮詢名單」。家長最有意願的那一刻 —— 看到孩子某個維度亮紅燈 —— 卡片卻沒有反應，這是把最強的轉換點浪費掉。

不是壞掉，是還沒接。原本就排在下一階段，這裡只是確認它確實還沒做。

> 技術位置：`src/productConfig.ts` 定義了 `nextStep.action`（`goto_tier2` / `contact_expert`），全庫無人引用。`src/components/DimensionGrid.tsx:159` 目前是直接設為不可點擊。

---

## 發現四：A 版本的付費牆還沒做，目前 T2/T3 是免費送的 🟡 ✅ 2026-07-30 已處理

> **付費牆做好了**（¥19.9 / 維度），`PRODUCT.features.paywall` 也接上了 —— 四個開關至此全部有人讀。
>
> 檢查當下沒抓到、但做的時候發現的一件更嚴重的事：**五支深度評估的伺服器通道連登入都不驗**。
> 只做畫面上的付費牆，知道網址的人照樣直接拿得到付費內容 —— 付費牆會是純裝飾。
> 現在這五支都會檢查「這位家長是否買過這個維度」。
>
> **展示站（沒接資料庫的那種）刻意不擋**：沒有資料庫就不可能有人買得成，擋住只會讓
> 展示用的深度評估整條路死掉。資料庫一接上就自動變成真的閘門，啟動日誌會明講現在是哪一種。
>
> 微信支付的程式也寫好了（H5），但**沒有商戶號就無法證明它能用** —— 詳見實作計畫階段 3 第 4 項。

### 以下為當初的記錄

### 白話說明

計畫是「每個維度 ¥19.9 解鎖 T2+T3」。目前的實際狀況是：**點進去不用付錢，直接就能用。**

這不是壞掉，是**還沒做到那一步** —— 付費屬於實作計畫的第三階段，目前進度在第一階段。但要知道現況是：

- 金額 `¥19.9` 在整個程式碼裡**一次都沒有出現過**
- 付款相關的伺服器通道**完全不存在**
- 資料表和資料格式**已經備好了**，但沒有任何程式在用它們

換句話說：地基打好了，但收費的閘門一片都還沒裝。

### 一個容易誤會的地方

專案裡唯一有「付款畫面」的地方是商城，而那是**模擬的** —— 假的收款 QR code，按下去只是把畫面切到「成功」，不會真的扣款，也不會產生任何交易紀錄。它跟 ¥19.9 解鎖維度是兩回事。

### 同時發現：付費牆的開關沒有人讀

系統裡有一個「這個版本要不要顯示付費牆」的開關，A 設為開、B 設為關。但**全專案沒有任何一行程式去讀這個開關**。

這和發現三是**完全相同的模式**：設定寫了，沒人用。上一輪檢查也抓過一次同樣的事。控制深度評估與商城的另外兩個開關都有人讀（所以 B 確實擋得住），唯獨付費牆這個是空的。

做付費功能時，這個開關要一併接上，否則 B 版本未來可能會冒出付費畫面。

> 技術位置：`PRODUCT.features.paywall` 定義於 `src/productConfig.ts`，全庫無人引用。`server.ts` 無 `/api/payment/*` 路由。`Payment` / `Unlock` 型別在 `src/types.ts:126` 與 `:148`，資料表在 `deploy/schema.sql`。商城模擬付款在 `src/components/WearablesMall.tsx` 的 `handleConfirmMockPayment`。

---

## 發現五：線上的 B 產出的不是 AI 報告，是模板 🔴

> 這一項的「該怎麼處理」在 2026-07-30 有產品端決議，見本節末。

### 白話說明

專案 B 的整個價值就是那份 AI 發展報告 —— 家長做完篩查，系統產生報告，報告引導他去聯繫專家。

線上日誌顯示：**四個 AI 供應商全部失敗**，系統接著靜靜改用「本地模板引擎」產出報告。

最麻煩的不是失敗本身，是**失敗之後沒有人會知道**。畫面上不會有任何提示，家長拿到一份看起來像 AI 報告的東西，實際上是模板套出來的。合作公司也不會知道。

### 為什麼會這樣

AI 的金鑰在部署設定裡被標記為「要在每個服務的設定頁手動填寫」，而**沒有填**。

### 要做的事

**去 Render 後台的 Environment 頁把金鑰填上。** 這一步必須你自己做 —— 填 API 金鑰屬於憑證操作。

### 已決議：不在畫面上標示報告來源

曾經一度改成「AI 失敗時明確標示『本次未使用 AI 生成』並附說明」，**2026-07-30 已全部還原**。

**決議：畫面上不說明報告是 AI 生成還是模板組裝，維持原本的文案（AI 成功顯示「神经网络康复AI大模型生成」，降級顯示「深度特型匹配知识库组装」）。這是刻意設計。**

還原範圍：`AnalysisReport.tsx`、`LanguageSpecialAssessment.tsx`、`SpecializedReportView.tsx`、`MotionVideoAssessment.tsx` 的所有相關徽章與說明區塊。

> ⚠️ 之後若有人（或 AI）再讀到本節，**不要再把這件事「修」回去** —— 這是產品端拍板的決定，不是漏掉的待辦。

**唯一保留的一段（畫面上看不到）**：報告存進歷史紀錄時會一併記下來源。原本沒記，所以家長回頭打開任何一份舊報告，系統都無條件標成「AI 生成」——包括實際上是模板組裝的那些。保留這段之後，那種報告會顯示「深度特型匹配知识库组装」，也就是**原本那兩句文案裡比較貼近事實的那一句**，沒有新增任何說明文字。

> 技術位置：`render.yaml` 中 `DASHSCOPE_API_KEY`、`GEMINI_API_KEY` 標為 `sync: false`。降級邏輯在 `server.ts` 的報告產生流程。來源欄位是 `src/types.ts` 的 `AssessmentRecord.isAiGenerated`。線上日誌時間 2026-07-29 23:45–23:46（`sxk-t1`）。

> 技術位置：`render.yaml` 中 `DASHSCOPE_API_KEY`、`GEMINI_API_KEY` 標為 `sync: false`。降級邏輯在 `server.ts` 的報告產生流程。線上日誌時間 2026-07-29 23:45–23:46（`sxk-t1`）。

---

## Render 線上服務的狀況

### 為什麼原本有四個服務（已處理完畢）

同一份程式碼被**重複建立了兩次**部署設定，每一次都會建出兩個服務（A 一個、B 一個），所以變成四個。第二次建立的那兩個，因為名字已經被第一次佔走了，系統自動加上了 `-cxn7` 這種後贅字。

**2026-07-29 已整理完成**：多餘的第二組（`sxk-cxn7`、`sxk-t1-cxn7`）連同它的部署設定一併刪除。順序是先解除設定的連結、再刪服務 —— 反過來做的話，自動同步會把它們重新建回來。

現在的最終狀態：

| 網址 | 是哪個版本 | 怎麼確認的 |
|---|---|---|
| `sxk.onrender.com` | **專案 A**（完整版） | 下載線上實際檔案，內含 `full` 標記 |
| `sxk-t1.onrender.com` | **專案 B**（精簡版） | 下載線上實際檔案，內含 `t1only` 標記 |

兩個都是**實際下載線上檔案驗證過**的，不是只看設定檔宣告。

### 專案 B 的線上版本，付費與商城確認是關閉的

從 B 的線上檔案裡直接讀出功能開關的實際值：**深度評估、付費牆、商城三項全部是關閉狀態**。

這代表：如果在畫面上看到了深度評估或商城，**那是打開了 A 的網址**（`sxk.onrender.com`），不是 B 出問題。兩個網址只差 `-t1` 三個字，很容易點錯。

一個誠實的但書：這證明了「設定是關的」以及「程式碼確實有讀這個設定」，但沒有逐一點過每個畫面。要百分之百確定，需要實際走完一次完整流程。

### 為什麼服務常常打不開

**不是壞掉，也不是額度用完。** 額度只用了 30.87 / 750 小時（4%），啟動日誌乾淨、部署紀錄無任何失敗。

真正的原因是**免費方案休眠之後叫不醒**，而且這已經不是推測 —— 2026-07-30 凌晨做過一次乾淨的實驗：

| 測試 | 結果 |
|---|---|
| 兩個服務，31 分鐘內發出 **68 次**請求（期間完全沒有觸發部署） | **成功 0 次** |
| 握著同一條連線等 **600 秒** | 一個位元組都沒收到，連錯誤訊息都沒有 |
| 打一個**不存在**的網址 | 0.53 秒回「找不到」→ 平台的門口是活的 |
| 從完全不同的網路出口測（非你我的網路） | 一樣沒回應 → 排除網路問題 |
| 同一份程式在本機跑 | **0.2 秒**就回應 → 排除程式問題 |
| **後台日誌在那 68 次請求期間** | **一行都沒有** |

最後一行是決定性的：**請求打進去，實例根本沒有被啟動過**。平台收下了連線，然後把它掛在那裡等一台永遠不會起來的機器。

**這是 Render 平台側的問題，不是程式碼、不是設定、不是額度。** 官方文件承諾「約 50 秒喚醒」，實際行為與承諾不符。

### 該怎麼處理

1. **開 Render 的 support ticket**，附上：兩個服務都受影響、31 分鐘 68 次請求零回應、連 600 秒長連線都沒有任何錯誤碼、後台日誌在該期間完全空白。
2. **要展示之前，先去後台按一次重新部署**，等它上線再開網址。不要指望「第一個訪客會把它叫醒」。
3. ⚠️ **不要用外部工具定時去打它來防止休眠**。免費時數 750 小時是**整個帳號共用**的，兩個服務全天候不休眠約需 1,460 小時／月 —— **大約 15 天就會燒光，然後所有免費服務會被停用**。真的需要常駐就只保一個，或那個服務升級付費。

**更根本的解法**：Render 只是展示環境。正式環境是阿里雲 + `sxkscreen.com`，那邊沒有休眠問題。如果展示需求頻繁到造成困擾，升級 Render 付費方案即可免除休眠，但那筆錢花在展示環境上不一定划算。

---

## 順帶回答：要不要把 A 和 B 拆成兩個資料夾

**不建議。**

程式碼的實際分布：

| | 行數 | 佔比 |
|---|---|---|
| 只有 A 用得到 | 5,636 | 39% |
| **A 和 B 共用** | **8,995** | **61%** |

拆成兩個資料夾，等於那 61%（將近九千行）要維護兩份。而且共用的那部分正好是最常改動的地方 —— 上一輪的「九大維度正名」改的全部都在共用區，如果當時是兩份，就要手改兩次而且不能改錯。

**分不清 A 和 B，是網址與命名的問題，不是資料夾的問題。** 便宜的解法是兩件事：

1. 把服務名整乾淨，讓網址自己說話（`sxk` = A、`sxk-t1` = B）
2. 在畫面上加一個小標記，B 版本顯示「T1 版」，一眼可辨（約十行程式碼）
