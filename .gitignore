# Dependencies
node_modules/

# Build output
dist/
server.js

# Secrets — never commit API keys
.env
.env.local
.env.*.local

# Editor / OS
.DS_Store
Thumbs.db

# Local tooling
.claude/

# Preview files
.preview
.cozeproj/reduction
.codegraph

# 報告／簡報等產出物 —— 二進位檔，每次更新都是一整份，不進版控
*.pdf

# 部署暫存：A 與 B 兩份產物分開放，避免共用 dist/ 時一邊傳一邊重建
deploy-out/
