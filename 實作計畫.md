# 森心康 SXK — A/B 雙專案實作計畫

> 更新：2026-07-28（階段 1 已完成，狀態已回填）
> 相關文件：[CONTEXT.md](./CONTEXT.md)（詞彙表）、[與客戶討論的問題.md](./與客戶討論的問題.md)（超出開發範圍的待決事項）

---

## Context

現有 SXK 是一套兒童發展篩查系統：家長完成分層問卷，系統呼叫 LLM 產生發展報告。目前程式碼在 Render 上運行（`https://sxk.onrender.com`，**無資料庫、記憶體模式**），阿里雲環境（主機 + RDS + 網域 `sxkscreen.com`）於 2026-07-16 前後購入但**遷移尚未完成**。

現在要做兩件事：

1. **專案 A**（現有產品）加上**付費解鎖深度評估**與**手機簡訊登入**，這需要完成阿里雲遷移與 ICP 備案，因為微信支付要求已備案網域。
2. **專案 B**（新產品）從 A 精簡而來，流程止於聯繫專家、不含付費，交付合作公司使用。

過程中查證發現數項既有缺陷（報告指標算錯、維度命名錯位、隱私條款承諾未實作），一併納入。

---

## 兩專案結構

| | **專案 A** | **專案 B** |
|---|---|---|
| 網域 | `sxkscreen.com` | `t1.sxkscreen.com`（子網域**不需另外備案**） |
| 流程 | 篩查 → 報告 → 深度評估（付費）→ 商城 | 篩查 → 報告 → **聯繫專家 → 結束** |
| 登入 | 純簡訊驗證碼、**無密碼** | **不登入**（匿名，預約時填手機）⚠️*暫定* |
| 付費 | 9 維度 × ¥19.9，單獨解鎖 T2+T3 | 無 |
| 資料庫 | `sxk_db` | **`sxk_t1_db`**（同一 RDS 實例，零額外成本） |
| 程式碼 | **共用一份**，以 `productConfig` 分歧 | 同左 |
| 部署 | 獨立 PM2 程序 | 獨立 PM2 程序 |

**架構原則**：程式碼共用（省維護）、資料與執行期分離（省風險）。共用檔案約 7,050 行，其中 `AnalysisReport.tsx`（1,436 行，含專家預約）是兩專案共同核心——fork 會使其維護成本加倍。

**建置指令**：

```bash
VITE_APP_MODE=full   pnpm build   # 專案 A
VITE_APP_MODE=t1only pnpm build   # 專案 B
```

---

## 階段 0：環境 ⬅️ **當前阻塞，需在伺服器上操作**

**關鍵事實：ICP 備案只擋「用網域對外提供網站服務」，不擋主機、更不擋資料庫。**
實測 `http://47.116.141.185` 回 502（nginx 有回應、Node 沒跑）而非被攔截 → 用 IP 存取即可開發驗證。

1. **阿里雲主機跑起來**：`pm2 start deploy/ecosystem.config.js`，設定 `MYSQL_*` 環境變數接上 RDS
2. **修 `deploy/nginx.conf`**：`server_name` 仍是佔位符 `your-domain.com`，改為真實網域 + 加子網域 server block
3. **建立 `sxk_t1_db`**：同一 RDS 實例新建資料庫，套用 `deploy/schema.sql`
4. **補 HTTPS**：備案通過後申請憑證、啟用 `nginx.conf` 中被註解的 HTTPS 區塊
5. **建置改在本機/CI 進行**：主機僅 2 GiB RAM，兩個 PM2 程序 + vite/esbuild 同時跑容易 OOM

> ICP 備案已送出。審核期間阿里雲會來電核驗，務必接聽，否則退件重來。

**驗收**：`curl http://47.116.141.185/api/db/status` 回 `{"configured":true,"engine":"mysql"}`（目前 Render 回 `memory`）。

---

## 階段 1：程式碼整頓 ✅ **已完成**

型別檢查通過、29 個測試全過、A/B 兩種模式皆建置成功。

### 1-1. 維度正名 ✅

`fine_motor → sensory_processing`、`sensory → emotion_behavior`、`family_env → learning_ability`。連帶修正四個算錯的指標：

| 指標 | 原本錯誤來源 | 修正後 |
|---|---|---|
| 感覺統合指數（`server.ts` + `reportUtils.ts`） | 情緒與行為 | 感覺處理 |
| 運動控制指數（同上兩處） | 混入感覺處理 | 僅動作發展，權重合併後區間維持 61–81 |
| 腦區圖「多感官皮質中樞」`relatedDims` | 情緒與行為 | 感覺處理 |
| 商城 OT 協作標籤 | 與 `data.ts` 矛盾 | 對齊正式名稱 |

**過程中發現兩項新的債**（已加 `NOTE` 註解，詳見 [CONTEXT.md](./CONTEXT.md) 的「已知的詞彙債」）：

- **T2/T3 內容仍停在舊分類體系** —— 分類改版時 `t1Data.ts` 篩查題目跟著改寫，深度評估沒有。`sensory_processing` 的採集任務仍是「畫筆握姿」（精細動作）。**這是內容問題，須待真實 T2/T3 題目到位時一併重寫，不可只改 case 名稱。**
- **「家庭環境」指標已無對應維度** —— 分類改版後無此維度，該指標目前取自「學習能力」，名稱與來源不一致，待產品端決定。

> 未寫舊 ID 轉換程式 —— 已確認資料庫無真實使用者，不需要。

**`/code-review` 補抓到三處漏網（2026-07-29 已修）**：

| 漏網 | 後果 | 修正 |
|---|---|---|
| 腦區拓樸圖把 `sensory` 節點改指 `sensory_processing` 後，**`emotion_behavior` 不再屬於任何節點** | 情緒與行為亮紅燈時，整張腦區圖仍全綠 | 加進 `limbic` 節點（該節點文案本就在講情緒共情與情緒穩健度） |
| 商城 `smart_lamp`（體態／步幅腰帶）被機械改名成標「情緒與行為」 | 商品卡顯示錯誤的 OT 協作標籤 | 改為 `gross_motor` |
| `t1Data.ts` 五處 `// 8. sensory ->` 分組註解未跟著改 | 稽核改名時 grep 會誤判仍有 `sensory` 維度 | 一併更新 |

**根本修法：[`test/dimensionIds.test.ts`](./test/dimensionIds.test.ts)**（11 條斷言）。維度 ID 散在六張以字串為 key 的表裡，`tsc` 一條都擋不住 —— 這次改名就是這樣讓 `emotion_behavior` 從腦區圖消失而 29 個測試全過。測試已實測會擋（把 bug 放回去即 fail）。

為了讓測試拿得到資料，三張表移進新檔 [`src/dimensionContent.ts`](./src/dimensionContent.ts)：`DIMENSION_DETAILS`（原 `AnalysisReport.tsx` 的 `DOMAIN_DETAILS`，順帶正名，「領域」是詞彙表列為避免的用詞）、`BRAIN_NODES`（原在 `ReportCharts.tsx` 元件函式內，順帶省掉每次 render 重建）、`REHAB_SUGGESTIONS`（原在 `server.ts` 函式內）。

> 腦區圖刻意不涵蓋 `self_care` 與 `learning_ability`（跨腦區的綜合表現，硬塞進某節點會讓該節點失真）。這個決定寫在 `BRAIN_NODE_UNCOVERED_DIMS`，測試會擋住**非刻意**的遺漏。

### 1-2. `tiers.T1` 標記廢棄 ✅

`types.ts` 的 `DimensionConfig.tiers.T1` 加上 `@deprecated`，`data.ts` 加檔頭說明。**篩查題目唯一來源是 `t1Data.ts`。**

### 1-3. 交易模型三詞分家 ✅

- `Order` → **`MallOrder`**（商城訂單，含收件與物流）
- 新增 **`Payment`**（付款紀錄）：`outTradeNo`、`transactionId`、`amountFen`、狀態
- 新增 **`Unlock`**（解鎖權益）：綁 `userId + dimensionId`，**永久有效**，含 `source`（`payment` / `grant`）與 `revokedAt`

**Payment 與 Unlock 分開的三個理由**（寫在型別註解裡）：支付回調會重複送達、退款需保留交易憑證、客訴補償需發權益而無交易。

### 1-4. `productConfig` 分歧模組 ✅

新增 [`src/productConfig.ts`](./src/productConfig.ts) 與 [`src/vite-env.d.ts`](./src/vite-env.d.ts)，10 處分歧收斂成單一入口，涵蓋：報告警示句、維度行動標籤、圖例提示、SECTION 4 標題與副標、建議量表區塊開關、篩查完成鈕文案、功能旗標。

大型 A 專屬元件改 `React.lazy`，實測 chunk 切分：

```
B (t1only)：主 chunk 466 kB
            + AssessmentPanel 54.7 kB / LanguageSpecialAssessment 81.9 kB
            / SpecializedReportView 50.8 kB / WearablesMall 22.6 kB  ← 共約 210 kB，B 永不下載
```

**誠實的限制**：檔案仍存在於建置產物中，只是不載入不執行。要連檔案都排除只能 fork，代價是 7,050 行維護兩次。

#### `/code-review` 發現：旗標當時**根本沒人讀**（2026-07-29 部分修正）

`features.mall` / `tier2And3` / `paywall` / `nextStep.action` 四個欄位定義了卻從未被引用 —— 當時只做了文案替換。B 的導覽列仍有「商城」、點維度卡片照樣進 T2/T3、`WearablesMall` 的 chunk 照樣下載。**驗證表那行「B 的頁面無任何 T2/付費/商城入口 ✅」在修正前並不成立。**

> ⚠️ **四個欄位只修好兩個。** 2026-07-30 重新 grep 全庫確認：
>
> | 欄位 | 現況 |
> |---|---|
> | `features.mall` | ✅ 已接上（路由層 + 入口層） |
> | `features.tier2And3` | ✅ 已接上（路由層 + 入口層） |
> | `features.paywall` | ✅ **2026-07-30 已接上**（見階段 3 第 3 項）。四個欄位至此全部有人讀 |
> | `nextStep.action` | ✅ **2026-07-30 已接上**（見階段 2 第 3 項）。B 的維度卡片改為可點，導向報告頁的專家預約區塊 |
>
> 原本的標題寫「已修」而底下列了四個欄位，會讓人以為四個都完成了。

修法分三層（缺一層就會漏）：

1. **路由層**：`currentView === 'mall' && PRODUCT.features.mall`、`'assessment' / 'specialized_report' / 'language_special' && PRODUCT.features.tier2And3`。就算入口漏改或 state 被弄壞，也只會掉到既有的 fallback view。對應後端「不註冊路由」的同一個想法。
2. **入口層**：導覽列商城鈕、客服選單的「商城訂單明細」整段、報告庫的 T2/T3 專項報告面板、維度卡片點擊（B 下不可點）、`SpecializedReportView` 的 `onGoToMall`。
3. **文案層**：另外掃出 **7 處**寫死的 A 專屬文案（使用說明第二步 + VIP 徽章、T1 入口說明、九維卡片區說明、卡片子標籤、卡片底部提示、篩查結論引導句、分數卡的「推薦 T2+T3 深度」徽章），全部收進 `productConfig`。

另外兩項：

- **`resolveMode()` 改為 fail-closed** —— 認不得的 `VITE_APP_MODE` 直接拋錯，不再靜靜退回 `full`。並在 [`vite.config.ts`](./vite.config.ts) 加上**建置期**檢查，因為 `productConfig` 的檢查跑在瀏覽器裡，打錯字的建置會照常成功、直到使用者開頁面才變空白。實測 `VITE_APP_MODE=t1_only pnpm build` 現在 exit 1。
- **`React.lazy` 補上 ErrorBoundary**（[`src/components/LazyBoundary.tsx`](./src/components/LazyBoundary.tsx)）。Suspense 只處理 pending 不處理 rejected —— 重新部署後 chunk 檔名雜湊改變、或手機網路斷一下，dynamic import reject 會讓 React 一路 unwind 到根節點，整頁空白且無路可退。現在是可重試的錯誤卡片。

#### 後端 `APP_MODE` 開關 ✅（2026-07-30 完成）

先前 `server.ts` 完全沒有 A/B 之分 —— B 的伺服器照樣註冊四個深度評估專用的 AI 端點，且無身分驗證。前端擋住了畫面，後端沒擋。

作法：`resolveAppMode()` 讀 `process.env.APP_MODE`，**fail-closed**（認不得的值直接讓程序起不來，比照前端的 `resolveMode()`）。深度評估端點註冊到 `tier2Only` —— A 模式下它就是 `app`，B 模式下是一個**從未被 `app.use()` 掛載的 Router**，等同於路由不存在。

> 為何是「不註冊」而非「註冊後擋下」：沒有 handler 就沒有 handler 可以被繞過；而且 404 不像 403 會反過來確認端點存在。

**實跑驗證**（起兩次服務、實際打端點）：

| 端點 | `APP_MODE=t1only` | `APP_MODE=full` |
|---|---|---|
| `/api/report`（A/B 共用） | 400 | 400 |
| `/api/specialized-report` | **404** | 400 |
| `/api/motion-eval` | **404** | 400 |
| `/api/motion-report` | **404** | 400 |
| `/api/ali-language-eval` | **404** | 400 |
| `/api/asr` | **404** | 400 |

400 = 路由存在但空 body 被拒；404 = 路由不存在。另外 `APP_MODE=t1_only`（打錯字）實測 exit 1。

啟動時會印出模式與**埠號來源**：`APP_MODE=t1only (project B — tier-2/3 endpoints NOT registered), port from PORT`。埠號來源會印出來是因為 `DEPLOY_RUN_PORT` 優先於 `PORT`（[server.ts:18](./server.ts)）—— 若有人把它填進 Render 的 Environment 頁，服務會連不上而且沒有其他症狀。

**階段 3 的付費路由要註冊在 `tier2Only` 上**（或比照新增一個 `paidOnly`），這個開關就是為此先落地的。

### 1-5. 資料表變更 ✅

[`deploy/schema.sql`](./deploy/schema.sql) 已重寫：

- `users`：`email` / `password` 放寬為 NULL，新增 `phone` 唯一鍵；移除明文種子帳號 `test@test.com`
- 新增 `sms_codes`（只存驗證碼杂湊、含防刷欄位）
- 新增 `payments`、`unlocks`、`expert_bookings`
- `unlocks` 有 `UNIQUE(user_id, dimension_id)` —— **這是冪等的關鍵**：微信回調重送時第二次開通會直接失敗
- 檔尾附既有資料庫的 `ALTER` 遷移語句（已註解，確認後再執行；先備份 RDS）

### 1-6. 移除明文測試帳號 ✅（`/code-review` 發現，2026-07-29）(明文帳號demo階段保留)

1-5 從 `schema.sql` 刪掉了種子帳號，但**同一組帳密在兩個地方活了下來**：

- `server.ts` 的記憶體備援仍 `offlineUsers.set('test@test.com', { password: '123456' })`
- `AuthScreen.tsx` 有一顆「使用一鍵填充 `test@test.com` 帳號」按鈕，把帳密直接印在登入頁上


> **命名說明**：新表未加 `t1_` 前綴。`T1` 在本專案指「篩查層級」，`t1_payments` 會被誤讀成「第一層的付款」。

---

## 階段 2：專案 B（下一步，只需備案）

1. **B 的匿名流程**：沿用現有 `getUserDataByDevice`（`deviceId` 存取不需 token），開發量幾乎為零
2. **專家預約做成真的** ✅ **2026-07-30 程式完成**（上線仍缺三項外部設定，見下）

   原本送出只做 `setBookingStatus('success')` —— 家長看到「預約成功確認書」，但**沒有任何人被通知、沒有留下任何紀錄**。現在改為：

   | 層 | 內容 |
   |---|---|
   | 端點 | `POST /api/expert-booking`，A/B 皆註冊（預約流程在共用的 `AnalysisReport` 裡，而且是 B 唯一的轉換點） |
   | 驗證 | 專家必填、家長姓名 1–64 字、手機須符合 `^1[3-9]\d{9}$`（工作人員要回撥，格式錯的預約等於死件） |
   | 寫入 | `expert_bookings`；未設 `MYSQL_*` 時退到記憶體並在回應帶 `persisted: false` |
   | 通知 | `src/notify.ts` 雙軌併發：企業微信群機器人 + 簡訊。**兩軌全掛會在日誌 `console.error` 大聲喊**（家長已經被告知專家會聯絡） |
   | 前端 | 送出改為等後端確認才切成功畫面，失敗顯示錯誤 —— **不再給假的成功**。送出中顯示 spinner |
   | 轉接 | 成功畫面新增微信客服二維碼／微信號區塊，文案與圖片路徑收在 `productConfig.expertBooking` |

   **設計取捨**：先確保寫入成功才回應，通知則不等（`res.json()` 之後才 fire）。理由是預約已經落地、工作人員隨時能按 `created_at` 撈單，讓家長多等一個 webhook round-trip 更糟。至少一軌成功才回填 `notified_at`。

   **速率限制**：`/api/expert-booking` 另設 1 小時 10 次（`RATE_BOOKING_MAX` 可調），比 `/api` 的 800 次嚴得多 —— 這支會觸發真人通知，被灌就是騷擾工作人員。

   ⛔ **上線前必須補的三項**（都不是程式問題）：

   1. **`deploy/schema.sql` 從未套用到線上資料庫。** 2026-07-30 實測連上 `sxk_db`，`expert_bookings` 表**不存在**（`payments`、`unlocks`、`sms_codes` 同樣沒有）。不套用的話這功能在正式環境會直接 500。
   2. **`WECOM_WEBHOOK_URL` 未設定** —— 沒設就沒有任何通知送出。這是最便宜的一軌（一個網址，免申請免費用）。⚠️ 訊息格式依企業微信群機器人的文字訊息契約撰寫，**尚未對真實 webhook 實測過**，格式若不符修正範圍僅限 `notifyWeCom()` 一個函式。
   3. **`PRODUCT.expertBooking.wechatId` 目前是佔位值** `SXK-KEFU-PLACEHOLDER`，二維碼圖片也還沒有。

   ⚠️ **阿里雲簡訊那一軌刻意未實作。** 它需要對正規化查詢字串做簽名，外加送審的簽名與模板（1–2 個工作天）。沒有憑證可驗證就寫簽名程式，等於交付一段看起來完成、實際無法證明正確的密碼學程式碼 —— 那比留一個明顯的缺口更糟。目前行為是**顯式的**：沒憑證就印在日誌；憑證有設但傳輸層未實作時會 `console.error` 拒絕，不會讓人誤以為簡訊有送出。

   ⚠️ **一句業務承諾要複查**：成功畫面寫「專家專屬評估顧問將在 **10 分鐘內**進行電話回訪」。現在預約是真的了，這句話就變成真實承諾。搭配 [與客戶討論的問題.md](./與客戶討論的問題.md) 第六項（三位專家是否知情並同意接受真實預約）一併確認。

   > 專家名單沿用現有 `SPECIALISTS`（寫死，不建後台），未改動。
3. ~~**`DimensionGrid` 點擊去向**：依 `PRODUCT.nextStep.action` 分流~~ ✅ **2026-07-30 完成**

   `nextStep.action` 從「定義了沒人讀」變成真的分流：A 進該維度的深度評估，B 導向報告頁的專家預約區塊。三處改動 ——

   - `DimensionGrid` 的 `disabled` 由 `!isT1Completed || !PRODUCT.features.tier2And3` 改為**只看 `!isT1Completed`**，B 的卡片 T1 完成後即可點；卡片文案新增 `dashboard.dimensionCardHint` 與 `dimensionCardCta`（A：「点击进入本维度测定」／「立即深测」，B：「点击联系专家说明」／「联系专家」）。該元件已完全不再引用 `features.tier2And3`。
   - `App.tsx` 的 `onSelectDimension` 依 `PRODUCT.nextStep.action` 分流，新增 `focusBooking` state。
   - `AnalysisReport` 新增 `focusBooking` prop，**等 `aiReport` 就緒後**才 `scrollIntoView` —— 預約區塊在 `{aiReport && ...}` 裡，掛載當下 ref 還是 null。

   > 為何是捲到區塊而不是直接彈出預約視窗：預約彈窗沒有維度上下文，家長點的是「語言溝通」卻跳出一個通用表單會失去脈絡。捲到 CTA 卡片保留了報告內容。

   **順帶修掉 `.claude/launch.json` 的一個缺漏**：兩個本機預覽都沒設 `APP_MODE`，後端會落到預設 `full` —— B 的預覽仍註冊 T2/T3 端點。補上後實測 5002 的 `/api/specialized-report`、`/api/ali-language-eval`、`/api/asr`、`/api/motion-eval` 皆回 404，`/api/report` 回 400（存在）。

   ⚠️ **未完成的驗證**：維度卡片點下去是否真的捲到預約區塊，需要一個完成 T1 的帳號才走得到，而 agent 不做密碼認證。lint、40 個測試、A/B 兩種建置皆通過，但這一段**尚未實機點過**。
4. **B 部署**：獨立 PM2 程序 + `sxk_t1_db` + `t1.sxkscreen.com` server block

---

## 階段 3：專案 A 付費

**不需等備案的部分**：

1. **純簡訊驗證碼登入**：阿里雲短信簽名/模板送審約 1–2 天。無密碼設計——輸入手機號 → 收碼 → 登入（首次自動註冊）。沿用現有 `signToken`/`verifyToken`。
   **必做防刷**：同號 60 秒限一次、同 IP 每日上限、驗證碼 5 分鐘失效、錯誤 5 次鎖定
2. **`Payment` / `Unlock` 的後端邏輯** ✅ **2026-07-30 完成**（微信下單那一段除外，見第 4 點）

   **官方契約已對齊**（用微信支付官方 skill 的本機文件庫查證，非憑記憶）：

   | 項目 | 官方規則 | 來源 |
   |---|---|---|
   | `out_trade_no` | `string(32)`，**6～32 字元**，只能是數字、大小寫字母、`_` `-` `|` `*`，同商戶號下唯一 | [4012791834](https://pay.weixin.qq.com/doc/v3/merchant/4012791834) |
   | `amount.total` | 整型，**單位為分**，須大於 0（1 元 = `100`）→ ¥19.9 = `1990` | 同上 |
   | `trade_state` | `SUCCESS` / `REFUND` / `NOTPAY` / `CLOSED` / `REVOKED` / `USERPAYING` / `PAYERROR`（後三者僅付款碼支付） | [4012791900](https://pay.weixin.qq.com/doc/v3/merchant/4012791900) |

   > 字元集**不含冒號與點** —— 訂單號用 `toISOString()` 會直接違規，而且要到使用者按下付款那一刻才被微信擋下。這條規則已用 [`test/outTradeNo.test.ts`](./test/outTradeNo.test.ts)（16 條斷言）釘住，含反向確認驗證器真的擋得住 ISO 字串。產生器在 [`src/utils/outTradeNo.ts`](./src/utils/outTradeNo.ts)，格式 `SXK` + `yyyyMMddHHmmss` + 8 位隨機碼 = 25 字元。

   **資料層**（`src/db/mysql.ts`）：`createPayment` / `findPaymentByOutTradeNo` / `markPaymentSuccess` / `markPaymentRefunded` / `grantUnlock` / `revokeUnlock` / `listUnlockedDimensions`。

   兩個關鍵設計：

   - **`markPaymentSuccess` 用條件更新做冪等** —— `WHERE out_trade_no = ? AND status = 'pending'`，只有真正完成狀態轉移的那一次回 `true`。微信回調最多重送 15 次，加上回跳頁還會觸發查單補償，多條路徑會同時搶著結算同一筆單；有這個條件只會有一個贏家，其餘看到 `affectedRows = 0` 就不得重複發放權益。
   - **`grantUnlock` 是 upsert 而非 insert** —— `unlocks` 有 `UNIQUE(user_id, dimension_id)`，退款後重購必須沿用既有列並把 `revoked_at` 清回 `NULL`。單純 `INSERT` 會撞唯一鍵失敗，家長就付了錢卻沒拿到東西。

   **路由**：新增 `paidOnly` 閘門（與 `tier2Only` 並列）。兩者目前解析結果相同，但「有深度評估」與「對它收費」是兩件不同的產品事實 —— 若哪天有 T2/T3 免費的試點版本就會分歧。

   | 端點 | 用途 |
   |---|---|
   | `GET /api/unlocks` | 已解鎖的維度清單。**這是存取權的唯一真實來源**，絕不採信前端存的任何東西 |
   | `POST /api/payment/create` | 為某維度開一筆待付款單，回傳商戶訂單號 |

   `/api/payment/create` 的三個防護：已擁有該維度就直接回 `alreadyUnlocked`（家長連點兩次或回到舊的付費頁不會重複付款）；未接資料庫時**回 503 拒絕**（沒有持久訂單紀錄就收錢，等於預約一場退款爭議）；產生的訂單號會先過官方契約驗證才寫入。

   ⛔ **刻意沒有任何「完成付款」的端點。** 唯一合法的觸發只有驗簽通過的微信回調與查單補償路徑。開一個 mock 完成端點，等於把付費內容送給任何找到那個網址的人。

   **順帶修掉一個會誤導前端的 bug**：`app.get('*')` 這個 SPA 萬用路由會把**所有未註冊的 GET `/api/*` 接走並回 200 + HTML**。也就是 B 上那些「已停用」的 GET 端點，對前端看起來像成功，前端會拿一份網頁去 `JSON.parse`。POST 之所以正確回 404，只是因為萬用路由僅接 GET —— 這讓 bug 在一半的表面上是隱形的。現在 `/api` 下未匹配的路徑一律回 JSON 404，SPA 首頁不受影響（實測 `/` 仍 200）。
3. **付費牆前端** ✅ **2026-07-30 完成**

   家長點被鎖住的維度 → 付費牆 → 付款 → 解鎖該維度 T2 + T3。四層：

   | 層 | 內容 |
   |---|---|
   | **後端強制** | 五支深度評估端點（`/api/specialized-report`、`/api/motion-eval`、`/api/motion-report`、`/api/ali-language-eval`、`/api/asr`）新增 `denyIfLocked`：驗 token → 查 `unlocks` → 未擁有回 403 `LOCKED`。**這一層才是真正的閘門**；先前這五支連 token 都不驗，只做前端付費牆等於裝飾 |
   | **存取決策** | [`src/utils/access.ts`](./src/utils/access.ts) 的純函式 `getDimensionAccess`，[16 條測試](./test/access.test.ts)涵蓋 B 模式／展示模式／已解鎖／未解鎖／兩種「尚未確定」 |
   | **付費牆畫面** | [`src/components/Paywall.tsx`](./src/components/Paywall.tsx)（lazy，A 專屬）。價格來自後端 `GET /api/unlocks` 的 `priceFen`，前端不寫死金額 |
   | **入口與路由** | 維度卡片顯示 `¥19.9 解锁` 徽章；`assessment` 與 `language_special` 兩條路由都再檢查一次，state 被弄壞也進不去 |

   **展示模式的刻意設計**：後端未接資料庫時 `denyIfLocked` 全部放行、`/api/unlocks` 回 `available:false`、前端不顯示任何價格。理由是沒有持久層就不可能有人買得成（`/api/payment/create` 同樣回 503），擋住只會讓展示站的深度評估整條路死掉。`MYSQL_*` 一設定就自動變成真的閘門，**啟動日誌會明講現在是哪一種**（`Paywall ENFORCED` / `NOT enforced ... Demo mode only`）。

   > **「尚未確定」一律當作未解鎖。** 查詢還沒回來就放行，等於每次重新整理都開一個免費視窗 —— 重整、搶在回應之前點進去即可。這條有專門的測試釘住。

   **實跑驗證**：MySQL 已設定時九張卡片全顯示 `¥19.9 解锁`、點下去進付費牆、下單失敗顯示錯誤而非假成功；未設定時無任何價格、直接進 T2。B 的畫面零付費字樣，`/api/unlocks`、`/api/payment/create`、`/api/payment/status`、`/api/pay/wechat/notify` 全部 404。

**重做篩查的處理**（`App.tsx:751` 目前只清 T1、保留 T2/T3）：

- **Unlock 綁 `user + dimension`，永久有效**，不綁篩查批次
- 舊 T2/T3 分數保留，但報告需用 `completedAt` 明確標示「此深度評估完成於 YYYY-MM-DD，早於本次篩查」
- 已解鎖維度可**免費重做** T2/T3

> 為何重要：`t1Data.ts` 年齡段為 12–23 個月、24–47 個月……三個月足以讓幼兒跨越年齡段，題目與判準都換一套。舊 T2/T3 與新 T1 並列而不標示時間，收費後即為退款與信任問題。

**必須等備案的部分**：

4. **微信支付串接** —— 程式 ✅ **2026-07-30 完成（H5）**，但**無法證明能用**，見下

   依 [`docs/research/wechat-pay-v3-h5.md`](./docs/research/wechat-pay-v3-h5.md)（逐條引官方文件網址）實作於 [`src/wechatPay.ts`](./src/wechatPay.ts)：

   | 部分 | 內容 |
   |---|---|
   | 請求簽名 | 五行簽名串 + `SHA256withRSA` + `WECHATPAY2-SHA256-RSA2048` header |
   | 統一下單 | `POST /v3/pay/transactions/h5`，金額單位分，回 `h5_url`（**效期 5 分鐘**） |
   | 回調 | `POST /api/pay/wechat/notify` —— **先驗簽再解密**、`Wechatpay-Serial` 比對、5 分鐘重放窗、`AEAD_AES_256_GCM` 解密 |
   | 冪等 | 回調與查單共用 `settlePayment()`，靠 `markPaymentSuccess` 的條件更新分出唯一贏家 |
   | 查單補償 | `GET /api/payment/status` —— 回調全部落空時唯一的救援路徑，且只有訂單擁有者問得到 |
   | 應答 | 成功 `204` 空 body、失敗 `4XX/5XX` + `{"code":"FAIL"}`（現行官方寫法，非網路流傳的 `{"code":"SUCCESS"}`） |

   ⚠️ **這段程式從未對微信伺服器跑過** —— 商戶號與備案都還不存在。已驗證的是「演算法與規格一致」，不是「串接成功」，這是兩件事。[`test/wechatPay.test.ts`](./test/wechatPay.test.ts) 22 條把簽名串釘在**微信自己公布的範例**上（含 GET 空 body 那行的 `\n` 不可省、CRLF 會壞掉、body 差一個空格就驗不過），並用本地金鑰實跑驗簽與 AES-GCM 解密。回調端點也用真實簽章的請求實打過：正確簽名 `204`、假簽名／serial 不符／時間戳過期／body 竄改一律 `401 FAIL`。

   **H5 vs JSAPI**：兩者是**互斥場景**不是包含關係 —— JSAPI 只能在微信內建瀏覽器調起（需 openid），H5 只能在微信外（官方原文：「H5支付不能直接在微信客户端内调起」）。目前實作 H5，JSAPI 的位置留好了（`TRADE_TYPE_PATHS` 與 `buildOrderBody` 都已分流，簽名／回調／查單／冪等全共用），缺的只有取得 openid 的網頁授權流程 —— `PAY_TRADE_TYPE=jsapi` 目前回 `501 JSAPI_NOT_IMPLEMENTED`，不會假裝可用。

   ⛔ **上線前的硬性前置**（全部來自官方文件，不是「之後再補」）：

   1. **已認證的公眾帳號 APPID** 並與商戶號綁定 —— 官方明文「未认证的账号无法绑定商户号」。JSAPI 更需要**已認證服務號**
   2. **商戶號**（企業主體，個人不可），且申請**虛擬服務類目**（不綁商城，避免資質問題拖累整體申請）
   3. **H5 支付域名配置**，審核 **3–5 個工作天**，且備案主體須與商戶號主體一致
   4. **APIv3 密鑰**（32 位）+ **商戶 API 證書**（私鑰 + 序列號）+ **微信支付公鑰**（官方推薦，免去平台證書輪換那一整套）
   5. **公網 HTTPS 的 `notify_url`**，不可帶查詢參數、不可為內網位址

   ⚠️ **H5 支付會檢查 Referer**：跳轉必須從已配置的 H5 支付域名下的網頁發起，域名**大小寫都要完全一致**。前端用一般導頁（`window.location.href`），**不可**用 `window.open(..., 'noreferrer')`，頁面也不能設 `Referrer-Policy: no-referrer` —— 少了 Referer 會直接失敗。
   ⚠️ **回跳不等於付款成功**。前端從微信回來後只做一件事：拿訂單號問後端查單。

   > ⚠️ 商戶憑證（APIv3 金鑰、商戶私鑰、證書）**由你本人在伺服器設定**，不進版控。環境變數清單見 [`.env.example`](./.env.example)，Render 的兩個 service 見 [`render.yaml`](./render.yaml)（B 刻意沒有任何 `WECHATPAY_*`）。

---

## 階段 4：商城真實交易（第二階段）

商品為醫療器械／保健食品／醫學檢驗性質，需對應經營資質，且微信支付屬特殊類目。
**先確認資質再開** —— 詳見 [與客戶討論的問題.md](./與客戶討論的問題.md) 第八項。技術上 `MallOrder` 模型已就緒。

---

## 驗證方式

| 項目 | 怎麼驗 |
|---|---|
| 環境 | `curl http://47.116.141.185/api/db/status` 應回 `{"configured":true,"engine":"mysql"}` |
| 維度正名 ✅ | `pnpm test`（40 passed，含 `dimensionIds.test.ts` 的 11 條一致性斷言）。已實測把 bug 放回去會 fail |
| productConfig ✅ | **實跑驗證**（非僅建置成功）：`VITE_APP_MODE=t1only pnpm build` 後以 `NODE_ENV=production node dist/server.cjs` 起站，走完一次 T1 → 報告。B 的導覽列只有「評估面板／評估報告」、九張維度卡片全部 `disabled`、客服選單無商城區塊、報告頁標題為「標記領域說明與**專家諮詢方向**」、建議量表區塊不存在，全頁無 `T2`/`T3`/`第二层`/`商城` 字樣。A 建置同法回歸：商城鈕、VIP 徽章、卡片可點皆正常 |
| 腦區圖 ✅ | 只讓「情緒與行為」亮紅燈，確認 `边缘情感及社交网络` 節點轉為 rose、其餘四節點維持綠色 |
| fail-closed ✅ | `VITE_APP_MODE=t1_only pnpm build` 應 exit 1 並印出可辨識的錯誤訊息 |
| 簡訊登入 | 真實中國門號收碼；驗證防刷（連按、換 IP） |
| 專家預約 | 送出後確認 `expert_bookings` 有資料、簡訊送達、企業微信群收到推播 |
| 付費牆 ✅ | `pnpm test`（99 passed）+ 實跑：設定 `MYSQL_*` 起站 → 九張卡片顯示 `¥19.9 解锁` → 付費牆 → 下單失敗顯示錯誤；未設定則無任何價格且直接進 T2。五支端點無 token 一律 401 `UNAUTHENTICATED` |
| 微信支付簽名 ✅ | `test/wechatPay.test.ts`（22 條，釘在官方公布範例上）+ 用本地金鑰簽出真實回調實打端點：正確 204、假簽名/serial 不符/時間戳過期/body 竄改一律 401 |
| 微信支付串接 | ⛔ **尚未驗證，也無法驗證** —— 需真實商戶號。取得後：沙箱先驗，重點測**回調重送不會重複開通**、退款後權益撤銷、H5 的 Referer 與域名大小寫 |
| 型別 | `pnpm run lint`（`tsc --noEmit`） |

---

## 順帶必須處理

- **`.env` 含真實密鑰**：DashScope API key 與線上 MySQL 帳密在工作目錄中（已 gitignore）。~~確認未曾 commit 進 git 歷史~~ ✅ 2026-07-29 查證：`git log --all -- .env` 為空、`git ls-files` 只有 `.env.example`，**從未進過版控**。仍建議輪換 key 與 DB 密碼（曾在多個 session 的工作目錄中）。
- **沒有安裝 `@types/react`**：`node_modules/@types/` 裡沒有 react。這代表 `pnpm run lint`（`tsc --noEmit`）**檢查不到任何 React API 與元件 props** —— 所有 `.tsx` 裡的 React 呼叫都是 `any`。「型別檢查通過」的保障比字面上弱得多。裝上去大概會噴出一批既有錯誤，所以沒有在階段 1 順手處理，但這是接下來值得排的一件事。
- **AI 端點無身分驗證**：`/api/report` 等端點無 `verifyToken`，僅有 IP 速率限制（10 分鐘 200 次）。這是**現存暴露**，見 [與客戶討論的問題.md](./與客戶討論的問題.md) 第四項。
- **隱私條款四項承諾未實作**：HTTPS、監護人同意、第三方分享同意、撤回權。見同一份文件第三項。
