/**
 * 進程進入點 —— 唯一會綁定連接埠的地方。
 *
 * `server.ts` 只負責定義應用程式（中介層、路由、閘門），載入它不會碰到網路；
 * 綁定連接埠是這個檔案的事。分開的理由不是整潔，是可測試性：
 * 2026-07-31 那個付費閘門授權繞過（驗的欄位與回應的內容不是同一個）之所以
 * 101 個測試一條都沒攔住，就是因為沒有任何測試看得到伺服器 —— 而看不到的原因
 * 是 `import server.ts` 會當場開始監聽。
 */
import { startServer } from './server';

startServer();
